# Copy to .env locally. On Streamlit Community Cloud: App settings → Secrets (TOML), then reboot.

OPENAI_API_KEY=sk-...

# Optional nested form (Secrets only) — either this block OR OPENAI_API_KEY above, not both required:
# [openai]
# api_key = "sk-..."
